import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class LoginAttemptService {

  constructor() { }


  addLogInAttempt(data) {

    // data:
    // failed username, password, date-time, publicIp[if possible]
    
  }
}
